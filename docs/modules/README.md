# docs/modules

Per-module documentation, one file per business module (`customers.md`, `leads.md`, ...), added
once a module has real behavior worth documenting beyond what's self-evident from its code and
its own `README.md`. Empty for now — no business modules exist yet (Phase 1 foundation only).
