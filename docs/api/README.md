# docs/api

API contracts (request/response shapes, error formats) for the eventual backend. Empty for now —
this project is frontend-only in its current phase; see
`docs/decisions/ADR-004-frontend-only-scope.md`. Populate once a backend/API is chosen.
