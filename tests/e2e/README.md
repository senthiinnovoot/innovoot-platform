# tests/e2e

Playwright end-to-end tests against a running build, covering critical
user flows across modules (e.g. sign in → create customer → book
appointment). Add these once the corresponding business flows exist —
there is little to cover end-to-end during the foundation stage.

Run: `pnpm test:e2e`
