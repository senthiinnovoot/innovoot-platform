# tests/integration

Tests that exercise a module's pieces together (page + hooks + services,
with the API layer mocked at the network boundary via MSW or similar) —
not a single unit, not a full browser.

Run: `pnpm test`
